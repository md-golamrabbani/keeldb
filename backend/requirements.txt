fastapi>=0.115
uvicorn[standard]>=0.32
sqlalchemy>=2.0
pymysql>=1.1
psycopg[binary]>=3.2
cryptography>=43.0
pydantic>=2.9
python-multipart>=0.0.12
sshtunnel>=0.4.0
# sshtunnel 0.4.0 references paramiko.DSSKey, removed in paramiko 4.0 — pin below it.
paramiko>=3.4,<4
# Optional: enables AI assist (natural-language → SQL) when ANTHROPIC_API_KEY is set.
anthropic>=0.40
keyring>=25
# CA trust for outbound HTTPS (AI providers) — prevents CERTIFICATE_VERIFY_FAILED
# on packaged builds and corporate/AV-proxied networks. truststore uses the OS
# trust store (incl. interception-proxy roots); certifi is the bundled fallback.
truststore>=0.9
certifi>=2024.0
# Bcrypt hashing for the opt-in Supabase Auth (auth.users) migration feature.
bcrypt>=4.0
